# trabajo-practico-integrador-3-fernando-laprida

# Este proyecto se trata de una integracion del backend con el frontend, capas de registrarse,logearse #con datos reales en la base de datos, teniendo las funcionalidades de crear, editar y eliminar las #tareas.
# Para instalar, se debera ejecutar un npm i en la consola para instalar las dependencias.
# La config del .env es:
DB_NAME=tp_integrador
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_DIALECT=mysql
DB_PORT=3306
JWT_SECRET=jwt_secret
PORT=3000
